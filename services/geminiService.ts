/**
 * Service for prompt operations.
 * AI optimization features have been disabled.
 */

export const optimizePromptDraft = async (draft: string): Promise<string> => {
  // Pass-through since AI optimization is removed
  return draft;
};